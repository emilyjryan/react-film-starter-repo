export default function Poster (props) {
        return (
            <>
                <img 
                    src={props.url} 
                    alt={props.alt}
                />
            </>
        )
}